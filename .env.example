# URLs de los formularios (Google Forms).
# Se leen en tiempo de BUILD (import.meta.env), así que deben estar definidas
# en el panel del hosting antes de compilar el proyecto.

# Formulario para quienes SÍ asisten.
PUBLIC_SURVEY_BIRTHDAY_URL=
# Formulario para quienes NO asisten.
PUBLIC_NOSURVEY_BIRTHDAY_URL=
