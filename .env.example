# URL del formulario de confirmación de asistencia (Google Forms).
# Se lee en tiempo de BUILD (import.meta.env), así que debe estar definida
# en el panel del hosting antes de compilar el proyecto.
PUBLIC_SURVEY_BIRTHDAY_URL=
